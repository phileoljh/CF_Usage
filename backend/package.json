{
  "name": "cf-usage-api",
  "version": "1.0.0",
  "description": "Cloudflare Usage API (BFF for Dashboard)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
